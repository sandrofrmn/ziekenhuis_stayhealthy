@model IEnumerable<Ziekenhuis_StayHealthy.Models.Afdeling>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Naam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Max_Patiënt)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Naam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Max_Patiënt)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Afdeling_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Afdeling_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Afdeling_ID })
        </td>
    </tr>
}

</table>
