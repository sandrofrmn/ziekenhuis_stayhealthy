@model IEnumerable<Ziekenhuis_StayHealthy.Models.Kamer>

@{
    ViewBag.Title = "Index";
    if (Session["function"] == null)
    {
        Response.Redirect("~/Home/Index");
    }
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Kamernaam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Afdeling_ID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Kamernaam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Afdeling_ID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Kamer_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Kamer_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Kamer_ID })
        </td>
    </tr>
}

</table>
